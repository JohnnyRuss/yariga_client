import React from "react";

import { TopAgent } from "components/Home";
import { PropertyCard } from "components/Layouts";
import { PieChart, PropertyReferrals, TotalRevenue } from "components/Charts";

interface HomePageT {}

const HomePage: React.FC<HomePageT> = (props) => {
  return <div>HomePage</div>;
};

export default HomePage;
